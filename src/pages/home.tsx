import { Footer } from "../components/footer"

export const Home = () => {
    return <div>
        <h1>Home</h1>
        <p>Click one of the schedule links to view a schedule.</p>
        <p>Changes to the HTML/css will update realtime.  Re-generating data currently requires application restart.</p>
    </div>
}
